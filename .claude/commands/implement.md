Use dd-i agent and implement design doc: $ARGUMENTS

## Instructions

1. Find the design doc matching "$ARGUMENTS" in `docs/`
2. If ambiguous — list matches, ask which one
3. Create and checkout new branch: `feat/{doc-name}` (e.g. `feat/001-schema-auth-org`)
4. Read the full doc + its index if exists (`docs/000-index.md`) for dependency context
5. Check dependency chain — confirm prerequisites are implemented (check codebase for existing code from prior docs)
6. Implement the COMPLETE spec from the doc using the dd-i agent
7. After implementation — summarize created/modified files